apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}


if (project.hasProperty('JDK7_HOME')) { options.bootClasspath = "$JDK7_HOME/jre/lib/rt.jar"}